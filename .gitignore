/.quarto/
**/*.quarto_ipynb
.Rproj.user
.Rhistory
.DS_Store
_output/
/modules/*_cache/
/modules/*_files/
/out/

# Machine-specific VSCode workspace (local R env paths)
*.code-workspace
/local_only/

# The original Gmail export of the statnet_help thread carries everyone's email
# addresses, so it stays local. The redacted Markdown reconstruction alongside it
# (ergm-statnet-2024.md) is tracked and published.
/correspondence/*.pdf
